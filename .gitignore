# Go build artefacts
/bin/
/dist/
*.test
*.out
coverage.out
token-server
github-app-token-server
# `go build ./...` names the binary after the module directory
gha-token-server

# Real client ACLs. Only clients.example.json belongs in the repo —
# the live file names actual callers and is rendered at deploy time.
clients.json
clients.*.json
!clients.example.json

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db

# Local config / secrets
.env
.env.local
*.pem
